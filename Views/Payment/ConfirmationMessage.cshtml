@model IEnumerable<BangazonDelta.Models.ConfirmationMessage>

@{
    ViewData["Title"] = "Confirmation Message Page";
}

<!--@foreach (var item in Model)-->

<!--{
    <div class="row">
        <div class="col-md-3">@Html.DisplayFor(modelPaymentType => PaymentType.AccountNumber)</div>
        <div class="col-md-3">@Html.DisplayFor(modelPaymentType => PaymentType.Expiration)</div>
        <div class="col-md-3">@Html.DisplayFor(modelPaymentType => PaymentType.CVV)</div>
        <div class="col-md-3"><a asp-action="Detail" asp-route-id="@item.ProductId">Details</a></div>
    </div>-->
}

{
    <div class="row">
        
        <p>Thank You For Your Order!</p>
        <br>
        <!--HOW DO I LINK THE ORDERID TO THE BELOW SECTION OF THE VIEW???-->
        <p>Order Number:[OrderId]</p>
        <br>
        <p>We are currently processing your order.  You should recieve it within 28-56 days.</p>
        <!--<div class="col-md-3"><a asp-action="Detail" asp-route-id="@item.ProductId">Details</a></div>-->
    </div>
}